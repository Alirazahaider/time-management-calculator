import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Time Management Calculator | Optimize Daily/Weekly Tasks",
  description: "Plan & visualize your tasks with this Next.js tool. Set priorities, track overbooking, and export summaries as PDF/PNG. Perfect for productivity!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
